---
title: 升级配置
---

[配置文件]: /trees/references/config.md

从旧版本迁移到新版本时, 用户也许希望自动适配最新的 [配置文件] 结构. 
对于这种需求, 可以使用 `upgrade` 命令. 执行 `kodama upgrade` 即可自动读取旧文件的信息并转换到新格式. 

如果 [配置文件] 名字不为默认的 `Kodama.toml`, 请使用可选参数 `--config` 指定. 如果希望保留新旧 [配置文件], 可通过 `--output` 指定新 [配置文件] 文件的路径. 
